# PizzaCalk - Калькулятор закупок пиццы

Универсальное приложение для расчета оптимального количества пицц и распределения стоимости между участниками заказа.

## 🍕 Функциональность

- **Умный расчет** - Автоматически рассчитывает оптимальное количество пицц
- **Учет предпочтений** - Каждый участник указывает свои предпочтения по количеству кусков
- **Бесплатные пиццы** - Учитывает акции и бесплатные пиццы при расчете стоимости
- **Пропорциональное распределение** - Справедливое распределение стоимости между участниками
- **Визуализация** - Наглядное представление заказа и распределения
- **Настройки** - Персональные настройки для каждого пользователя
- **Аутентификация** - Регистрация, вход и гостевой режим
- **История заказов** - Сохранение и просмотр предыдущих расчетов

## 🛠 Технологии

### Frontend
- **React 18** с TypeScript
- **Tailwind CSS** для стилизации
- **React Router** для навигации
- **Chart.js** для визуализации
- **Lucide React** для иконок
- **Vite** для сборки

### Backend
- **Node.js** с Express
- **MongoDB** с Mongoose
- **JWT** для аутентификации
- **bcryptjs** для хеширования паролей
- **TypeScript** для типизации

## 🚀 Быстрый старт

### Автоматическая установка (Windows)
```powershell
# Установка всех зависимостей
.\install-dependencies.ps1

# Запуск проекта
.\start-dev.ps1
```

### Ручная установка

1. **Установите зависимости:**
```bash
# Корневые зависимости
npm install

# Frontend
cd frontend
npm install
cd ..

# Backend
cd backend
npm install
cd ..
```

2. **Настройте переменные окружения:**
```bash
# Скопируйте файл примера
cp backend/env.example backend/.env

# Отредактируйте backend/.env
# Укажите MONGODB_URI и JWT_SECRET
```

3. **Запустите проект:**
```bash
# Запуск всего проекта
npm run dev

# Или отдельно:
# Frontend (http://localhost:5173)
cd frontend && npm run dev

# Backend (http://localhost:3001)
cd backend && npm run dev
```

## 📁 Структура проекта

```
PizzaCalk/
├── frontend/                 # React приложение
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── hooks/           # Пользовательские хуки
│   │   ├── utils/           # Утилиты
│   │   └── types/           # TypeScript типы
│   ├── public/              # Статические файлы
│   └── package.json
├── backend/                 # Node.js API
│   ├── src/
│   │   ├── controllers/     # Контроллеры
│   │   ├── models/          # Модели данных
│   │   ├── routes/          # Маршруты API
│   │   ├── middleware/     # Middleware
│   │   └── utils/           # Утилиты
│   └── package.json
├── shared/                  # Общие типы и классы
│   ├── types.ts            # TypeScript интерфейсы
│   └── classes.ts          # Бизнес-логика
└── README.md
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Профиль пользователя
- `PUT /api/auth/me` - Обновление профиля

### Заказы
- `POST /api/orders/calculate` - Расчет заказа
- `GET /api/orders/history` - История заказов
- `GET /api/orders/:id` - Детали заказа
- `DELETE /api/orders/:id` - Удаление заказа

### Настройки
- `GET /api/settings` - Получение настроек
- `PUT /api/settings` - Обновление настроек
- `POST /api/settings/reset` - Сброс настроек

## 🎯 Основные классы

### PizzaCalculator
Основной класс для расчета оптимального заказа:
- Расчет общего количества кусков
- Определение оптимального количества пицц
- Учет бесплатных пицц
- Распределение стоимости

### UserManager
Управление пользователями:
- Добавление/удаление участников
- Обновление предпочтений
- Управление персональными соусами

### SauceManager
Управление соусами:
- Персональные и общие соусы
- Расчет стоимости соусов
- Распределение между участниками

## 🎨 Особенности UI

- **Адаптивный дизайн** - Работает на всех устройствах
- **Темная/светлая тема** - Настраиваемые цвета
- **Интуитивный интерфейс** - Простое управление
- **Визуализация данных** - Графики и диаграммы
- **Экспорт результатов** - JSON, PDF (планируется)

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- Rate limiting для API
- Валидация входных данных
- CORS настройки

## 📊 Мониторинг

- Логирование запросов
- Обработка ошибок
- Health check endpoint
- Статистика использования

## 🚀 Развертывание

### Локальная разработка
```bash
npm run dev
```

### Продакшн
```bash
# Сборка frontend
cd frontend && npm run build

# Запуск backend
cd backend && npm start
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для функции
3. Внесите изменения
4. Создайте Pull Request

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию
2. Создайте Issue в репозитории
3. Обратитесь к команде разработки
