# Функциональность PizzaCalk

## 🎯 Основные возможности

### 1. Управление участниками

**Добавление участника:**
- Поле ввода всегда доступно под списком участников
- Имя можно не вводить - автоматически будет "User N"
- Автодополнение из сохраненных пользователей
- Количество кусков по умолчанию: 3
- Чекбокс "Можно больше" - позволяет получить дополнительные куски

**Редактирование:**
- Кнопки +/- для изменения количества требуемых кусков
- Диапазон: 1-20 кусков
- Чекбокс "Можно больше" включает/выключает возможность получения лишних кусков
- Кнопка удаления участника

**Результаты под каждым участником:**
```
Хочет: 3 кусков (то, что выбрал пользователь)
Получит: 5 кусков (+2) (фактически после распределения)
```

### 2. Расчет на лету

**Автоматический расчет при изменении:**
- Количества участников
- Количества кусков
- Состояния чекбокса "Можно больше"

**Показываются:**
- Нужно кусков (сумма требуемых)
- Количество пицц
- Бесплатных пицц (каждая 3-я)
- Лишних кусков (если есть)

### 3. Варианты расчета

**Если лишних кусков 1-4 (меньше половины пиццы):**

Показываются два кликабельных варианта:

**Вариант 1 - "С лишними кусками":**
- Текущее количество пицц
- Бесплатных пицц
- Лишних кусков
- Лишние распределяются между участниками с "можно больше"

**Вариант 2 - "Убрать лишние":**
- На 1 пиццу меньше
- Меньше бесплатных пицц
- Показывает сколько кусков не хватит

**При клике на вариант:**
- Обновляется выбранный вариант (подсветка)
- Пересчитываются результаты для каждого участника
- Обновляется распределение кусков

**Если лишних кусков > 4:**
- Показывается только один вариант
- Уведомление о большом количестве лишних кусков

### 4. Алгоритм распределения лишних кусков

```javascript
pieces = totalPizzaSlices - totalMinSlices

For each user from users:
  If checkbox "можно больше" AND pieces > 0:
    actualSlices = min(pieces, maxSlices - currentSlices)
    user.actualSlices += actualSlices
    pieces -= actualSlices
    
    If pieces = 0:
      Exit
```

**Особенности:**
- Распределение идет по порядку добавления участников
- Каждому добавляется столько, сколько возможно (но не больше максимума)
- Процесс останавливается при исчерпании лишних кусков
- Пользователи без чекбокса не получают дополнительных кусков

### 5. Интерфейс

**Главный экран:**
- Список участников (всегда виден заголовок)
- Поле добавления нового участника
- Расчет на лету
- Информационный блок

**Нижняя панель (фиксированная):**
- Кнопка "Показать результат" (слева, когда есть участники)
- Кнопка настроек (справа, всегда)

**Настройки:**
- Кусков в маленькой пицце (по умолчанию 6)
- Кусков в большой пицце (по умолчанию 8)
- Цена маленькой пиццы (600₽)
- Цена большой пиццы (800₽)
- Каждая N-я пицца бесплатная (по умолчанию 3)
- Очистка сохраненных данных

### 6. Сохранение данных

**LocalStorage:**
- Сохраняются имена пользователей для автодополнения
- Можно очистить через настройки

**Автодополнение:**
- Срабатывает при начале ввода имени
- Показывает список сохраненных пользователей
- Клик по имени автоматически заполняет поле

## 📱 Мобильная оптимизация

- Максимальная ширина: 800px
- Адаптивные размеры кнопок (sm: для desktop, уменьшенные для mobile)
- Touch-friendly интерфейс
- Фиксированная нижняя панель
- Оптимизированные отступы для мобильных устройств

## 🎨 Цветовая схема

- **Синий** - количество пицц
- **Зеленый** - бесплатные пиццы, дополнительные куски
- **Оранжевый** - лишние куски
- **Красный** - недостающие куски, удаление
- **Серый** - неактивные элементы











