# Варианты расчета в PizzaCalk

## Обзор

Система предлагает несколько вариантов расчета, чтобы пользователь мог выбрать оптимальный для своей ситуации.

## Когда показываются варианты

### Вариант 1: Только большие пиццы (по умолчанию)
Всегда показывается. Основной расчет на основе больших пицц.

### Вариант 2: Маленькие пиццы
Показывается **ТОЛЬКО ЕСЛИ** количество кусков в маленькой и большой пицце **отличается**.

**Условие:**
```typescript
showSmallPizzaOption = smallPizzaSlices !== largePizzaSlices
```

**Пример:**
- Маленькая: 6 кусков
- Большая: 8 кусков
- → Показывается вариант с маленькими пиццами

### Вариант 3: Убрать лишние
Показывается **ТОЛЬКО ЕСЛИ** лишних кусков от 1 до 4 (меньше половины пиццы).

**Условие:**
```typescript
showReducedOption = extraSlices > 0 && extraSlices <= 4
```

## Структура вариантов

### Сценарий 1: Разные размеры пицц + нет лишних кусков
**Настройки:** Маленькая = 6, Большая = 8  
**Желаемые куски:** 16  
**Лишних:** 0

**Показываются 2 варианта:**
```
┌─────────────┬─────────────┐
│  Большие    │  Маленькие  │
│  2 пиццы    │  3 пиццы    │
│  0 бесплатн │  1 бесплатн │
└─────────────┴─────────────┘
```

### Сценарий 2: Одинаковые размеры + есть лишние 1-4
**Настройки:** Маленькая = 8, Большая = 8  
**Желаемые куски:** 6  
**Лишних:** 2

**Показываются 2 варианта:**
```
┌─────────────┬─────────────┐
│  Большие    │  -1 пицца   │
│  1 пицца    │  0 пицц     │
│  0 бесплатн │  -6 не хват │
│  2 лишних   │             │
└─────────────┴─────────────┘
```

### Сценарий 3: Разные размеры + есть лишние 1-4
**Настройки:** Маленькая = 6, Большая = 8  
**Желаемые куски:** 6  
**Лишних (большие):** 2

**Показываются 3 варианта:**
```
┌─────────────┬─────────────┬─────────────┐
│  Большие    │  Маленькие  │  -1 пицца   │
│  1 пицца    │  1 пицца    │  0 пицц     │
│  0 бесплатн │  0 бесплатн │  -6 не хват │
│  2 лишних   │  0 лишних   │             │
└─────────────┴─────────────┴─────────────┘
```

### Сценарий 4: Много лишних кусков (> 4)
**Желаемые куски:** 4  
**Пицц:** 1 (8 кусков)  
**Лишних:** 5

**Показывается 1 вариант:**
```
┌─────────────────────────┐
│  Обычный расчет         │
│  Нужно кусков: 4        │
│  Пицц: 1                │
│  Бесплатных: 0          │
│  Лишних: 5 (> половины) │
└─────────────────────────┘
```

## Интерактивность

### Выбор варианта
При клике на любой вариант:
1. Вариант подсвечивается (розовый фон + рамка)
2. Автоматически пересчитываются результаты для каждого пользователя
3. Обновляется распределение кусков
4. Показываются обновленные данные под каждым участником

### Визуальные индикаторы
- **Синий** - количество пицц
- **Зеленый** - бесплатные пиццы
- **Оранжевый** - лишние куски
- **Красный** - недостающие куски
- **Розовый фон** - выбранный вариант

## Формула определения вариантов

```typescript
const variants = []

// Вариант 1: Всегда показывается
variants.push('large')

// Вариант 2: Если размеры отличаются
if (smallPizzaSlices !== largePizzaSlices) {
  variants.push('small')
}

// Вариант 3: Если лишних 1-4
if (extraSlices > 0 && extraSlices <= 4) {
  variants.push('reduced')
}

// Итого: от 1 до 3 вариантов
```

## Логика расчета для каждого варианта

### Большие пиццы
```typescript
pizzaList = createPizzaList(largePizzaCount, useSmall=false)
totalSlices = sum(pizza.slices for pizza in pizzaList)
distribution = calculateDistribution(pizzaList)
```

### Маленькие пиццы
```typescript
pizzaList = createPizzaList(smallPizzaCount, useSmall=true)
totalSlices = sum(pizza.slices for pizza in pizzaList)
distribution = calculateDistribution(pizzaList)
```

### Убрать лишние
```typescript
pizzaList = createPizzaList(largePizzaCount - 1, useSmall=false)
totalSlices = sum(pizza.slices for pizza in pizzaList)
distribution = calculateDistribution(pizzaList)
// Показывает сколько не хватит
```

## Пример работы

**Исходные данные:**
- User 1: 3 куска, ✅ можно больше
- User 2: 2 куска, ❌ строго
- Настройки: Маленькая = 6, Большая = 8

**Желаемых кусков:** 5

**Вариант "Большие":**
- 1 пицца × 8 кусков = 8 кусков
- Лишних: 3
- User 1 получит: 5 (+2)
- User 2 получит: 2
- Лишних останется: 1

**Вариант "Маленькие":**
- 1 пицца × 6 кусков = 6 кусков
- Лишних: 1
- User 1 получит: 4 (+1)
- User 2 получит: 2
- Лишних останется: 0

**Вариант "-1 пицца":**
- 0 пицц × 8 = 0 кусков
- Не хватит: 5 кусков
- Этот вариант не подходит

Пользователь выбирает наиболее подходящий вариант! 🎯






